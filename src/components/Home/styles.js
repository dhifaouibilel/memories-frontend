import { makeStyles } from '@mui/styles';

// console.log(theme.breakpoints)
export default makeStyles({

    AppBarSearch: {
        borderRadius: 4,
        marginBottom: '1rem',
        display: 'flex',
        padding: 16
    },
    pagination: {
        borderRadius: 4,
        marginTop: '1rem',
        padding: 16
    },

})